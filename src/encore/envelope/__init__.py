"""Envelope computations. Phase 1: duration accounting (open-loop feasibility LPs).

Backward-reachability projection, bisection on F(x,c), readiness sets and the
virtual-battery reparameterization are Phase 2 (NOT in this run — guide Section 11).
"""
